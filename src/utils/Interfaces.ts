export interface singleItem {
    id: number
    name: string
    hashName: string
    borderColor: string
    image: string
    price: number
    priceMin: number
    priceAvg: number
    priveMax: number
    updatedAt: string
}